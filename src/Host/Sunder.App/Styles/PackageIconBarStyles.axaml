<Styles xmlns="https://github.com/avaloniaui"
        xmlns:controls="using:Sunder.App.Views.Controls">

  <Style Selector="MenuItem.package-bar-context-menu-item">
    <Setter Property="Foreground" Value="{DynamicResource Sunder.Brush.Foreground.Primary}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Padding" Value="10,7" />
    <Setter Property="MinWidth" Value="132" />
  </Style>

  <Style Selector="MenuItem.package-bar-context-menu-item:pointerover">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Surface.Selected}" />
    <Setter Property="Foreground" Value="{DynamicResource Sunder.Brush.Foreground.Primary}" />
  </Style>

  <Style Selector="Border.package-bar-host">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Transparent}" />
    <Setter Property="CornerRadius" Value="3" />
  </Style>

  <Style Selector="Border.package-bar-host.horizontal">
    <Setter Property="MinHeight" Value="40" />
  </Style>

  <Style Selector="Border.package-bar-host.drag-over">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Surface.DragOver}" />
  </Style>

  <Style Selector="Border.package-bar-host.horizontal.drag-over">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Info.Soft}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Sunder.Brush.Accent}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style Selector="controls|PackageIconBar.side-top-lane StackPanel.package-bar-vertical-content">
    <Setter Property="Margin" Value="0,10,0,0" />
  </Style>

  <Style Selector="controls|PackageIconBar.side-bottom-lane StackPanel.package-bar-vertical-content">
    <Setter Property="Margin" Value="0,0,0,10" />
  </Style>

  <Style Selector="Border.package-bar-item-host">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Transparent}" />
    <Setter Property="Padding" Value="2" />
    <Setter Property="CornerRadius" Value="3" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Transitions">
      <Transitions>
        <ThicknessTransition Property="Margin" Duration="0:0:0.08" />
      </Transitions>
    </Setter>
  </Style>

  <Style Selector="Border.package-bar-button-surface">
    <Setter Property="Width" Value="38" />
    <Setter Property="Height" Value="38" />
    <Setter Property="CornerRadius" Value="{DynamicResource Sunder.Radius.Small}" />
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Transparent}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Sunder.Brush.Transparent}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style Selector="Border.package-bar-button-surface.top-bar-surface">
    <Setter Property="Width" Value="36" />
    <Setter Property="Height" Value="36" />
  </Style>

  <Style Selector="Border.package-bar-item-host:pointerover Border.package-bar-button-surface">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Surface.Hover}" />
  </Style>

  <Style Selector="Border.package-bar-item-host.drag-preview">
    <Setter Property="Cursor" Value="Arrow" />
  </Style>

  <Style Selector="Border.package-bar-item-host.drag-preview Border.package-bar-button-surface.drag-preview">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Selection}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Sunder.Brush.Accent}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Opacity" Value="0.95" />
  </Style>

  <Style Selector="Border.package-bar-button-surface.selected">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Surface.Selected}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Sunder.Brush.Accent}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style Selector="Border.drag-ghost-surface">
    <Setter Property="Background" Value="{DynamicResource Sunder.Brush.Selection}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Sunder.Brush.Accent}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Opacity" Value="0.95" />
  </Style>

  <Style Selector="Button.package-bar-button">
    <Setter Property="Width" Value="38" />
    <Setter Property="Height" Value="38" />
  </Style>

  <Style Selector="Button.package-bar-button.top-bar-button">
    <Setter Property="Width" Value="36" />
    <Setter Property="Height" Value="36" />
  </Style>

  <Style Selector="Button.package-bar-button.overflow-button">
    <Setter Property="FontSize" Value="18" />
  </Style>

</Styles>
