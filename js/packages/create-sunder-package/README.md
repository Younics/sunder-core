# create-sunder-package

Run `npm create sunder-package@latest` to scaffold a TypeScript process Runtime package. Add `-- --template react-node` for a combined sandboxed React/Vite App and pinned Node SEA Runtime package.
